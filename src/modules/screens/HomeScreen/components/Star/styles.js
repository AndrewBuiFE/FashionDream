import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  starSection: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  star: {
    width: 14,
    height: 14,
  },
});
export default styles;
